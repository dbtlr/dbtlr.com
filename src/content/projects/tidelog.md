---
name: tidelog
blurb: Structured logging that stays readable in a terminal.
description: Structured logging that stays readable in a terminal. JSON on the wire, columns for your eyes.
short: structured logging
lang: Rust
status: active
version: v1.2
order: 2
tagline: Structured logging that stays readable in a terminal. JSON on the wire, columns for your eyes.
facts:
  - label: Status
    value: Active development
  - label: Install
    value: cargo add tidelog
    mono: true
  - label: Runtime
    value: Rust 1.75+
thumbLabel: tidelog — terminal output screenshot
shotLabel: tidelog — terminal output screenshot · drop image here
---

Placeholder body — replace with the real project write-up.
